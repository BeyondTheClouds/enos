Vagrant (VBox)
--------------

Workflow:

* Deploys OpenStack in a multinode mode using the Vagrant(VBox) provider.
* Check if a server can be booted and if Rally can be launched.


.. literalinclude:: ../../tests/functionnal/tests/vbox/vbox.yaml
    :language: yaml
    :name: vbox.yaml
    :caption: Configuration
